<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/shape_s_white_c_20"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="20dp">

    <!-- 내용 -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_contents"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/black_800"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="이것은 다이어로그 메뉴에요,\n괜찮아요?" />

    <!-- Positive Button -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_positive"
        android:layout_width="0dp"
        android:layout_height="44dp"
        android:gravity="center"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginTop="28dp"
        android:textColor="@color/white"
        android:background="@drawable/ripple_shape_s_black_700_c_12"
        app:layout_constraintTop_toBottomOf="@+id/tv_contents"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="확인" />

    <!-- Negative Button -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_negative"
        android:layout_width="0dp"
        android:layout_height="44dp"
        android:gravity="center"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginTop="10dp"
        android:background="@drawable/ripple_shape_s_white_c_12_l_black_700"
        app:layout_constraintTop_toBottomOf="@+id/tv_positive"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="취소" />

</androidx.constraintlayout.widget.ConstraintLayout>